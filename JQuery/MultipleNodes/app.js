$(document).ready(function () {
	//notation is just like CSS selectors
	// $("li").text("same thing");							//select DOM node
	// $("#groceries").text("one thig selected by ID");		//select by ID
	$(".antiDoctor").text("many apples");					//select by class
})